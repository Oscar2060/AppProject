<%= form_with(model: gym_product) do |form| %>
  <% if gym_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gym_product.errors.count, "error") %> prohibited this gym_product from being saved:</h2>

      <ul>
        <% gym_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :numInStock %>
    <%= form.text_field :numInStock %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
